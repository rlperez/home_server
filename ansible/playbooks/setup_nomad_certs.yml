---
- name: Rotate Nomad Certificates
  hosts: "{{ target_host | default('nomad_servers') }}"
  gather_facts: true
  become: true

  vars:
    nomad_cert_force: true

  roles:
    - vault_pki
    - nomad_certificates
